import { Routes } from '@angular/router';
import { LocationsComponent } from './pages/locations/locations.component';

export const routes: Routes = [ 
    { path:'locations-component', component: LocationsComponent },
];
